@{
    Layout = "_Layout";
}

@using Library.Models;
@model List<Library.Models.Author>;
<div class="container card">
<h1>Authors</h1>
<form>
    <i class="glyphicon glyphicon-search"></i><input type="text" name="AuthorSearch" placeholder="search and press enter">
</form>

@if (@Model.Count == 0)
{
  <h3>No authors have been added to library database</h3>
}

@foreach(Author author in Model)
{
  <li>@Html.ActionLink($"{author.AuthorName}", "Details", new { id = author.AuthorId })</li>
}


<p>@Html.ActionLink("Add new author", "Create")</p>
<input class="btn btn-info" type="button" value="Delete All Authors" onclick='location.href="@Url.Action("DeleteAll")";' />
<p>@Html.ActionLink("Home", "Index", "Home")</p>
</div>